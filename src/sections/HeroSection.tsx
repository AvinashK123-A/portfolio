// HeroSection removed — combined into AboutSection (Change #1)
// The About Me section now serves as the primary hero with profile image, intro, role, stats and CTA.
export default function HeroSection() {
    return null;
}
